# ML-VOT

```sh
usage: main.py [-h] [-n N_FRAME] [-g] [-H] [-a] [-v]

options:
  -h, --help            show this help message and exit
  -n N_FRAME, --n-frame N_FRAME
                        Number of frames to use
  -H, --hungarian       Use Hungarian algorithm
  -a, --all             Use all frames
  -v, --video           Create video

```

